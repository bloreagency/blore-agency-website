/** @type {import('postcss-load-config').Config} */
module.exports = {
  plugins: {
    // Tailwind CSS (utilities, components, base)
    tailwindcss: {},
    // Autoprefixer for better cross-browser support
    autoprefixer: {},
  },
};


